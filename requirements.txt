# Neo-Logos Training Dependencies
# Pinned versions for reproducibility

# Core API and environment
anthropic>=0.40.0
python-dotenv>=1.0.0
httpx>=0.27.0  # Required for API timeout configuration

# Machine Learning and Training
transformers>=4.44.0
datasets>=2.20.0
trl>=0.9.0
peft>=0.12.0
bitsandbytes>=0.43.0
huggingface-hub>=0.24.0
torch>=2.4.0
accelerate>=0.33.0

# Unsloth - CRITICAL for training
# Install separately if using CUDA: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
unsloth>=2024.9

# Utilities
numpy>=1.26.0
tqdm>=4.66.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
